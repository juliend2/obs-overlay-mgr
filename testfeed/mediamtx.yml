# Minimal MediaMTX config for a local test feed.
# Only RTSP (ingest) + WebRTC (playback) are on; everything else is off to keep
# the footprint small.

logLevel: info

api: false
metrics: false
pprof: false
playback: false
rtmp: false
srt: false
hls: false
moq: false

# ffmpeg publishes here
rtsp: true
rtspAddress: :8554
# UDP is kept enabled so RTSP readers (OBS's Media Source) connect without a
# "461 Unsupported Transport" round-trip. The default RTP ports are :8000/:8001,
# which collide numerically with the PHP dev server, so move them out of the way.
rtspTransports: [udp, tcp]
rtpAddress: :8100
rtcpAddress: :8101

# browser plays from here (WHEP)
webrtc: true
webrtcAddress: :8889
webrtcLocalUDPAddress: :8189

paths:
  # Codec choice is forced by this machine:
  #   * Firefox offers only VP8/VP9 in its WebRTC SDP (verified: no H.264 payload
  #     type in the offer, even with the OpenH264 2.6.0 GMP installed).
  #   * This CPU is a 2-core Celeron N3000 @1.04GHz. Measured libvpx VP8 realtime
  #     factors: 1280x720@30 = 0.50x (cannot keep up), 854x480@25 = 1.33x,
  #     640x360@30 = 1.58x.
  # So: VP8 at 854x480@25 by default, which leaves ~33% headroom.
  # runOnDemand means ffmpeg only runs while a viewer is connected.

  test:
    runOnDemand: >
      ffmpeg -hide_banner -loglevel error
      -re -f lavfi -i testsrc=size=854x480:rate=25
      -c:v libvpx -deadline realtime -cpu-used 16 -threads 2 -b:v 1M -g 25
      -f rtsp rtsp://127.0.0.1:8554/$MTX_PATH
    runOnDemandRestart: true
    runOnDemandCloseAfter: 10s

  # smoother motion, lower resolution — most headroom on this CPU
  smooth:
    runOnDemand: >
      ffmpeg -hide_banner -loglevel error
      -re -f lavfi -i testsrc=size=640x360:rate=30
      -c:v libvpx -deadline realtime -cpu-used 16 -threads 2 -b:v 800k -g 30
      -f rtsp rtsp://127.0.0.1:8554/$MTX_PATH
    runOnDemandRestart: true
    runOnDemandCloseAfter: 10s

  mandelbrot:
    runOnDemand: >
      ffmpeg -hide_banner -loglevel error
      -re -f lavfi -i mandelbrot=size=854x480:rate=25
      -c:v libvpx -deadline realtime -cpu-used 16 -threads 2 -b:v 1M -g 25
      -f rtsp rtsp://127.0.0.1:8554/$MTX_PATH
    runOnDemandRestart: true
    runOnDemandCloseAfter: 10s

  # H.264 720p is only 3.3x realtime to encode here (and free via VAAPI), but
  # Firefox will reject it. Usable if you ever install Chromium.
  h264:
    runOnDemand: >
      ffmpeg -hide_banner -loglevel error
      -re -f lavfi -i testsrc=size=1280x720:rate=30
      -c:v libx264 -preset ultrafast -tune zerolatency -profile:v baseline
      -pix_fmt yuv420p -g 30
      -f rtsp rtsp://127.0.0.1:8554/$MTX_PATH
    runOnDemandRestart: true
    runOnDemandCloseAfter: 10s
